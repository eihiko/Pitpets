<h1>Your Conversations</h1>

<p><%= link_to "new conversation", new_message_path %></p>
<p>
  ~<%= mailbox_section "inbox", @boxtype %> 
  ~<%= mailbox_section "sent", @boxtype %> 
  <%-# ~<%= mailbox_section "trash", @boxtype %>
</p>
<ul class="list-group">
  <%= render partial: 'conversations/conversation', collection: @conversations %>
</ul>

<%= will_paginate %>
